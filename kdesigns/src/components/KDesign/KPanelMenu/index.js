import { PanelMenu } from 'primereact/panelmenu';
import React from 'react';

function KPanelMenu({ className, model }) {
  return <PanelMenu model={model} className={className} />;
}

export default KPanelMenu;
